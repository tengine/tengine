# encoding: utf-8

前提 /^OSの種類は"([^"]*)"である$/ do |arg1|
  # pending # express the regexp above with the code you wish you had
end

前提 /^OSのバージョンは"([^"]*)"である$/ do |arg1|
  # pending # express the regexp above with the code you wish you had
end

前提 /^OSのビット幅は"([^"]*)"である$/ do |arg1|
  # pending # express the regexp above with the code you wish you had
end

前提 /^"([^"]*)"のバージョンは"([^"]*)"である$/ do |pkg, version|
  puts "#{pkg}は#{version}です"
  # pending # express the regexp above with the code you wish you had
end
